#!/usr/bin/env bash

PASS_FILE="${HOME}/.config/please.txt"
if [[ -f "${PASS_FILE}" ]]
then
  # Avoid the 'Password:' remainings
  cat "${PASS_FILE}" | sudo -S echo >/dev/null 2>&1
fi

[ "${#@}" == "0" ] && sudo -i || sudo $@
